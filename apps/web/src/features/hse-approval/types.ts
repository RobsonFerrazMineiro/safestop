import { hseApprovalQueryKeys } from "@safestop/query-keys";

/** Fila HSE — 30s (HSE-APPROVAL-UI-SPEC § HSE-QUEUE). */
export const HSE_APPROVAL_QUEUE_STALE_TIME_MS = 30_000;

export { hseApprovalQueryKeys };
